function compressBoxesTwice(boxes, boxes2){
    boxes.forEach(function(boxes){
        console.log(boxes);
    });

    boxes2.forEach(function(boxes){
        console.log(boxes);
    });
}
//Addition for loops next to each other of different terms
//O(a + b)
//Multiplication for nested loops of different terms
//O(a*b)